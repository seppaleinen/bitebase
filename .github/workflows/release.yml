name: Release

on:
  # Only run after CI passes on main — never release a broken build.
  # The workflow_run trigger fires when the named workflow completes;
  # the branch filter + conclusion check ensure we only proceed when
  # all tests are green on the main branch.
  workflow_run:
    workflows: ["CI"]
    branches: [main]
    types: [completed]

# Only one release job runs at a time — prevents race conditions on version tags
concurrency:
  group: release
  cancel-in-progress: false

jobs:
  # ── Guard: skip if the triggering CI run didn't succeed ───────────────────
  ci-gate:
    name: CI passed
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    steps:
      - run: echo "CI is green — proceeding with release"

  # ── Bump semantic version ──────────────────────────────────────────────────
  version:
    name: Bump version
    needs: ci-gate
    runs-on: ubuntu-latest
    permissions:
      contents: write  # needed to push the new git tag

    outputs:
      new_tag: ${{ steps.tag.outputs.new_tag }}
      previous_tag: ${{ steps.tag.outputs.previous_tag }}
      changelog: ${{ steps.tag.outputs.changelog }}

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0   # full history so the tag action can read all commits

      # Reads conventional commit messages and decides the version bump:
      #   feat: …          → minor (1.x.0)
      #   fix: …           → patch (1.0.x)
      #   BREAKING CHANGE  → major (x.0.0)
      #   chore|docs|…     → patch (default fallback)
      - name: Bump version and push tag
        id: tag
        uses: mathieudutour/github-tag-action@v6.2
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          default_bump: patch
          custom_release_rules: "feat:minor,fix:patch,BREAKING CHANGE:major"
          tag_prefix: "v"
          release_branches: main

  # ── Build Docker images per platform (native runners) ──────────────────
  # Each architecture builds on its native runner — no QEMU emulation.
  # amd64 uses ubuntu-latest (x86_64); arm64 uses ubuntu-24.04-arm (Graviton).
  # Images are pushed with platform-suffixed tags and later merged into
  # multi-arch manifests by the docker-merge job.
  docker-build:
    name: Build (${{ matrix.platform }})
    needs: version
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: linux/amd64
            suffix: amd64
            runner: ubuntu-latest
          - platform: linux/arm64
            suffix: arm64
            runner: ubuntu-24.04-arm
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
      packages: write   # needed to push to GHCR

    env:
      REGISTRY: ghcr.io
      IMAGE_NAME: ${{ github.repository }}

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract image metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          # We only need labels from this step; tags are set inline below
          flavor: |
            latest=false
          tags: |
            type=sha,format=short

      - name: Build and push runner image
        uses: docker/build-push-action@v6
        with:
          context: .
          target: runner
          push: true
          platforms: ${{ matrix.platform }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest-${{ matrix.suffix }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ needs.version.outputs.new_tag }}-${{ matrix.suffix }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}-${{ matrix.suffix }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push migrations image
        uses: docker/build-push-action@v6
        with:
          context: .
          target: builder
          push: true
          platforms: ${{ matrix.platform }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-migrate:latest-${{ matrix.suffix }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-migrate:${{ needs.version.outputs.new_tag }}-${{ matrix.suffix }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-migrate:${{ github.sha }}-${{ matrix.suffix }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ── Merge platform-specific images into multi-arch manifests ──────────────
  docker-merge:
    name: Merge multi-arch manifests
    needs: [version, docker-build]
    runs-on: ubuntu-latest
    permissions:
      packages: write

    env:
      REGISTRY: ghcr.io
      IMAGE_NAME: ${{ github.repository }}
      TAG: ${{ needs.version.outputs.new_tag }}

    steps:
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Create and push multi-arch manifests
        run: |
          set -euo pipefail

          # ── Runner image tags ──
          #   ghcr.io/<owner>/<repo>:latest
          #   ghcr.io/<owner>/<repo>:v0.19.7
          #   ghcr.io/<owner>/<repo>:0.19.7        (semver, no v prefix)
          #   ghcr.io/<owner>/<repo>:sha-e3370af
          docker buildx imagetools create \
            -t $REGISTRY/$IMAGE_NAME:latest \
            -t $REGISTRY/$IMAGE_NAME:$TAG \
            -t $REGISTRY/$IMAGE_NAME:${TAG#v} \
            -t $REGISTRY/$IMAGE_NAME:sha-${{ github.sha }} \
            $REGISTRY/$IMAGE_NAME:latest-amd64 \
            $REGISTRY/$IMAGE_NAME:latest-arm64

          # ── Migrations image tags ──
          #   ghcr.io/<owner>/<repo>-migrate:latest
          #   ghcr.io/<owner>/<repo>-migrate:v0.19.7
          #   ghcr.io/<owner>/<repo>-migrate:0.19.7
          #   ghcr.io/<owner>/<repo>-migrate:sha-e3370af
          docker buildx imagetools create \
            -t $REGISTRY/$IMAGE_NAME-migrate:latest \
            -t $REGISTRY/$IMAGE_NAME-migrate:$TAG \
            -t $REGISTRY/$IMAGE_NAME-migrate:${TAG#v} \
            -t $REGISTRY/$IMAGE_NAME-migrate:sha-${{ github.sha }} \
            $REGISTRY/$IMAGE_NAME-migrate:latest-amd64 \
            $REGISTRY/$IMAGE_NAME-migrate:latest-arm64

      - name: Image digests
        run: |
          echo "Runner image:    $REGISTRY/$IMAGE_NAME:$TAG"
          echo "Migrations image: $REGISTRY/$IMAGE_NAME-migrate:$TAG"

  # ── Create GitHub Release with auto-generated changelog ───────────────────
  github-release:
    name: Create GitHub Release
    needs: [version, docker-merge]
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Create release
        uses: ncipollo/release-action@v1
        with:
          tag: ${{ needs.version.outputs.new_tag }}
          name: ${{ needs.version.outputs.new_tag }}
          body: ${{ needs.version.outputs.changelog }}
          # Don't mark as pre-release since we push from main
          prerelease: false
